

/**
 * Interface of how the update objects in the Shape updatetor should look like
 */
interface updateInterface {
    cb: Function,
    min: number,
    max: number
}

export default updateInterface


const generateShapeStyles = (width: number, speed: number, delay: number) => {
    return (
        {
            // the css of a li element which is displayd in the dom as a shape
        }
    )

}

const bgStyles = {
    generateShapeStyles
}

export default bgStyles


const config = {
    colorOpacity: 0.1,

    minSpeed: 20,
    maxSpeed: 3,

    minSize: 5,
    maxSize: 15,

    minDelay: 0,
    maxDelay: 4,

    minLeft: 0,
    maxLeft: 85
}
export default config